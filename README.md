# minimal reproduced repo for https://github.com/nartc/mapper/issues/455


## If you comment out one line 70 or 77 in the file 'src/user/user.schema.ts', then the code will work.


## To start
Run `npm i`
Run `npm start`
Open `http://localhost:83/api/#/user/UserController_get`
